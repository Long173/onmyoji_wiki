# Supabase credentials for the uploader. The service_role key bypasses RLS,
# so this file MUST stay out of git (it's gitignored).
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
